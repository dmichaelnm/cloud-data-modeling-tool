/**
 * Represents the version information of the application.
 * This object contains semantic versioning details along with a build type.
 *
 * @property {string} major - The major version number, indicating significant changes.
 * @property {string} minor - The minor version number, representing incremental updates or new features.
 * @property {string} patch - The patch version number, used for small fixes or updates.
 * @property {string} build - The build type or environment, such as 'development' or 'production'.
 */
export const version = {
  major: '0',
  minor: '0',
  patch: '3',
  build: 'development',
};
